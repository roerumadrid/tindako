/** Re-export global toast helper (see `<Toaster />` in root layout). */
export {
  handleError,
  handleSuccess,
  toast,
  type HandleErrorOptions,
  type HandleSuccessOptions,
  type ToastPayload,
  type ToastVariant,
} from "@/lib/toast";
